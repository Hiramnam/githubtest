// async function f(){

// let promise = new Promise((resolve, reject) => {
//     setTimeout(() => resolve('Exito'), 3000);
// })
// const result = await promise;
// alert(promise);
// }

// Simulacion de una peticion asincrona

// async function f() {
//     let promise = new Promise((res, rej) => {
//         setTimeout(() => res('done'), 3000)
//     })
    
//     let result = await promise;
    
//     alert(result);
// }

// f();


/*
const btn = document.getElementById('btn-count');
const p = document.getElementById('contador');

let counter = 0;

btn.addEventListener('click', async () => {
    await setTimeout(() => {
        counter++;
        p.textContent = counter;
    }, 1000
    );

});


p.textContent = counter;

const btn2 = document.getElementById('btn2-rest');
const b = document.getElementById('restador');

let rest = 0;

btn2.addEventListener('click', async () => {
    await setTimeout(() => {
        rest--;
        p.textContent = counter;
    }, 1000
    );

});

*/




















